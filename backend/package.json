{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mvn package -DskipTests",
    "test": "mvn test",
    "dev": "docker compose up backend-dev",
    "clean": "mvn clean"
  }
}
